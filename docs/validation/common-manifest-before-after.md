# Common Manifest Before / After

Before: the repository used only the legacy JSON manifest.

After: `config/template.manifest.yaml` and its common schema are present. The
Template Base and manifest agree on template `1.1.0`; schema and data format
remain `1.0.0`. Fictional sample content is preserved and no internal SPEC or
AI history is included.

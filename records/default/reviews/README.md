# Reviews

Store narrative reviews as Markdown files. A review may cover a session,
assessment, issue, sprint, or goal. Photos are intentionally not referenced by
record ID; photos are grouped independently by date under `artifacts/<year>/`.
